# Daily 03/08/2026

เรื่องที่ 1: แก้ไขปัญหาสถานะการลงเวลาไม่อัปเดต (Cache Mismatch)
  - ประเภท: Bug Fix & Cache Management
  - สิ่งที่ทำ:
    - ยกเลิก Cache ประวัติลงเวลา (`getUserHistory`) ฝั่ง Server (Apps Script) เพื่อให้อ่านสดจาก Google Sheets เสมอ
    - ปรับปรุง client-side status check (`checkTodayStatus`) ให้ล้าง cache และส่ง `force_fresh` ไปยัง Server
    - เพิ่มระบบ Auto-Refresh ด้วย `visibilitychange` เมื่อผู้ใช้สลับกลับมาเปิดแอป
- ผลลัพธ์: ปัญหาเครื่องที่ยังไม่ได้เข้างานแต่ขึ้นว่าเข้างานครบแล้วหายไป และข้อมูลตรงกับ Google Sheets เสมอ

เรื่องที่ 2: แก้ไขปัญหาบันทึกข้อมูลไม่สำเร็จ (Google Sheets Cell Limit)
  - ประเภท: Critical Bug Fix & Stability
  - สิ่งที่ทำ:
    - บีบอัดรูปภาพเซลฟี่ 2 ชั้นด้วย Canvas (280px quality 0.45 และ 200px quality 0.3)
    - เพิ่มตัวตัดทอนข้อความรูปภาพ Base64 ไม่เกิน 49,000 ตัวอักษรใน `apps-script.gs` ก่อนบันทึกลง Sheet
    - ปรับปรุง POST fallback ไปยัง GET โดยตัดรูปภาพออกเพื่อไม่ให้ URL ทรุด
- ผลลัพธ์: แก้ไขปัญหาสคริปต์พังจาก Cell size limit (>50,000 chars) ทำให้กดบันทึกสำเร็จ 100%

เรื่องที่ 3: ปรับปรุงปุ่มล้างแคชและรีเฟรชบน Dashboard
  - ประเภท: Frontend Improvement & UX Fix
  - สิ่งที่ทำ:
    - แก้ไขพารามิเตอร์ของปุ่มรีเฟรชหมุนๆ ให้เรียก `forceRefreshDashboard(this)` เพื่อแก้ปัญหา Event Scoping บนมือถือ
    - ปรับปรุงข้อความ Loading Overlay ขณะรีเฟรชเป็น "กำลังอัปเดตข้อมูลล่าสุด..."
    - ประกาศตัวแปร `memoryCache` และเพิ่มฟังก์ชัน `clearInternalCache()` ล้างทิ้งทั้ง RAM และ LocalStorage
- ผลลัพธ์: ปุ่มรีเฟรชกดได้สมบูรณ์ ข้อความถูกต้อง และล้างแคชเพื่อดึงข้อมูลสอดคล้องกับ Sheet จริงทันที

เรื่องที่ 4: แก้ไขปัญหารูปเซลฟี่ไม่แสดงผลในบางเครื่อง
  - ประเภท: Version Management & Asset Cache Busting
  - สิ่งที่ทำ:
    - ปรับเปลี่ยนเวอร์ชันของแอปและสคริปต์เป็น `v1.3.6`
    - Bump script query parameter ในไฟล์ HTML เป็น `app.js?v=20260803-5`
    - ล้าง Key รูปภาพและประวัติเก่า (`attendance_photo_*`, `db_history_*`) อัตโนมัติเมื่อมีการอัปเดตเวอร์ชัน
- ผลลัพธ์: รูปเซลฟี่แสดงผลถูกต้องทุกคนบนทุกอุปกรณ์

เรื่องที่ 5: ปรับ UI ซ่อนเมนูแอดมินฝั่งผู้ใช้งาน
  - ประเภท: UI / UX Adjustment
  - สิ่งที่ทำ:
    - นำแท็บเมนู "แอดมิน" ออกจาก Bottom Navigation ในหน้า Dashboard และ History ของแอปพลิเคชัน
    - ปรับแต่งการสลับหน้าใน `app.js` ไม่ให้กระทบกับส่วนนำทางหลัก
- ผลลัพธ์: หน้าจอฝั่งผู้ใช้เรียบง่าย ไม่สับสน และไม่มีปุ่มแอดมินแสดงบนเมนูด้านล่าง

เรื่องที่ 6: Deployment และ Version Control
  - ประเภท: Deployment & Version Control
  - สิ่งที่ทำ:
    - ดันโค้ด Apps Script หลังบ้านด้วย `clasp push` เวอร์ชัน 1.3.6
    - Commit และ Push ข้อมูลการเปลี่ยนแปลงทั้งหมดเข้าสู่ Git Repository (Branch `main` และ `dev`)
- ผลลัพธ์: ระบบ Production อัปเดตพร้อมใช้งานและซิงค์ตรงกับระบบควบคุมเวอร์ชัน Git
