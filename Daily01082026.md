# Daily 01/08/2026

เรื่องที่ 1: ปรับปรุงความเร็วในการโหลดหน้าเว็บ
  - ประเภท: ปรับปรุงประสิทธิภาพ Frontend
  - สิ่งที่ทำ:
    - ลดไฟล์ JavaScript ที่ต้องโหลดตอนเปิดหน้าจาก 14 เหลือ 3 ไฟล์
    - เปลี่ยน Tailwind CDN เป็นไฟล์ CSS ที่ compile แล้ว
    - โหลดกล้อง แผนที่ และระบบส่งออก Excel เฉพาะเมื่อต้องใช้งาน
- ผลลัพธ์: ผู้ใช้เปิดหน้าเข้าสู่ระบบและหน้าแอดมินได้เร็วขึ้น และลดการประมวลผลที่ไม่จำเป็นบนมือถือ

เรื่องที่ 2: เพิ่มระบบ Index และ Cache สำหรับ Google Sheets
  - ประเภท: ปรับปรุงฐานข้อมูลและ Backend
  - สิ่งที่ทำ:
    - เพิ่ม application index สำหรับค้นหาผู้ใช้ด้วยรหัสผู้ใช้และชื่อเข้าสู่ระบบ
    - เพิ่ม CacheService พร้อม versioned cache invalidation
    - ลดการอ่านข้อมูลทั้งชีตในคำสั่ง Login, Edit และ Delete
- ผลลัพธ์: ค้นหาและจัดการผู้ใช้ได้เร็วขึ้น พร้อมลดจำนวนครั้งที่อ่าน Google Sheets

เรื่องที่ 3: ปรับ Query รายงานและประวัติการลงเวลา
  - ประเภท: Query Optimization
  - สิ่งที่ทำ:
    - เพิ่ม server-side filter และ pagination ครั้งละ 10 รายการ
    - อ่านเฉพาะคอลัมน์ที่จำเป็นสำหรับการกรองและส่งออกรายงาน
    - แยกการโหลดรูปยืนยันตัวตนออกจากข้อมูลรายงาน
- ผลลัพธ์: หน้าแอดมินไม่ต้องโหลด Attendance ทั้งชีตและรูป Base64 ทั้งหมดในครั้งเดียว

เรื่องที่ 4: ปรับปรุง CRUD และ Modal Popup
  - ประเภท: ปรับปรุงประสบการณ์ใช้งาน
  - สิ่งที่ทำ:
    - รวม request ที่ซ้ำกันและใช้ข้อมูลจาก cache ก่อน
    - อัปเดตเฉพาะผู้ใช้ที่เพิ่ม แก้ไข หรือลบ โดยไม่โหลดตารางใหม่ทั้งหมด
    - เพิ่มสถานะกำลังบันทึกและข้อความผิดพลาดที่อ่านเข้าใจง่าย
- ผลลัพธ์: Popup เพิ่ม แก้ไข และลบพนักงานเปิดและตอบสนองได้เร็วขึ้น

เรื่องที่ 5: ปรับ Responsive Design และ Accessibility
  - ประเภท: ปรับปรุงหน้าจอและการเข้าถึง
  - สิ่งที่ทำ:
    - ปรับ Modal และตารางสำหรับมือถือ แท็บเล็ต และ Desktop
    - เพิ่ม touch target ขั้นต่ำ 44px และรองรับ safe area ของโทรศัพท์
    - เปิดให้ผู้ใช้ซูมหน้าเว็บและเพิ่มข้อความกำกับรูปภาพ/ปุ่มสำคัญ
- ผลลัพธ์: ใช้งานบนหน้าจอขนาดเล็กได้ง่ายขึ้น โดย Popup ไม่ล้นจอและปุ่มสำคัญมองเห็นครบ

เรื่องที่ 6: เพิ่ม Popup แจ้งข่าวการอัปเดตระบบ
  - ประเภท: เพิ่มฟังก์ชันแจ้งเตือนผู้ใช้
  - สิ่งที่ทำ:
    - เพิ่มชีต `APP_UPDATES` สำหรับกำหนดวันที่ กลุ่มเป้าหมาย หัวข้อ และรายการเปลี่ยนแปลง
    - แยกข้อความสำหรับผู้ใช้ทั่วไปและแอดมิน
    - บันทึกสถานะการรับทราบตามผู้ใช้ บทบาท และเวอร์ชัน เพื่อไม่แสดงข้อความเดิมซ้ำ
- ผลลัพธ์: ผู้ใช้เห็นสรุปการอัปเดตแบบสั้นและเข้าใจง่าย พร้อมวันที่รูปแบบ `dd/mm/yyyy`

เรื่องที่ 7: อัปเดต Google Apps Script และ Deployment 1.3.1
  - ประเภท: Deployment
  - สิ่งที่ทำ:
    - บันทึก backend เวอร์ชัน 1.3.1 เข้าโปรเจกต์ Apps Script ที่เชื่อมกับฐานข้อมูลจริง
    - เลือกอัปเดต deployment เดิมเพื่อคง Web App URL เดิม
    - คงการทำงานในนามเจ้าของบัญชีและสิทธิ์เข้าถึงเป็น `ทุกคน`
    - เผยแพร่เป็น Apps Script เวอร์ชัน 42
- ผลลัพธ์: อัปเดต deployment เดิมโดยคง Web App URL เดิมไว้ และ API จริงตอบกลับเวอร์ชัน 1.3.1 เรียบร้อย

เรื่องที่ 8: ตรวจสอบคุณภาพก่อนเผยแพร่
  - ประเภท: Testing และ Quality Assurance
  - สิ่งที่ทำ:
    - ตรวจ syntax ของ Frontend และ Apps Script
    - ตรวจ Git diff และ HTML ID ซ้ำ
    - ทดสอบ Popup สำหรับผู้ใช้และแอดมินบนหน้าจอมือถือ 390×844
    - ทดสอบ Modal CRUD และหน้าแอดมินบน Desktop 1440×900
- ผลลัพธ์: ชุดโค้ดผ่านการตรวจสอบในเครื่องและ API จริงหลัง Deployment

เรื่องที่ 9: ปรับ Dropdown ฝั่งแอดมินให้พิมพ์ค้นหาได้
  - ประเภท: ปรับปรุง UI และ Accessibility
  - สิ่งที่ทำ:
    - เปลี่ยน dropdown กรองสาขา กรองพนักงาน และ dropdown ใน Modal เพิ่ม/แก้ไขพนักงานให้พิมพ์ค้นหาได้ทุกจุด
    - รองรับการค้นหาชื่อภาษาไทย การใช้งานด้วยคีย์บอร์ด และแสดงข้อความเมื่อไม่พบข้อมูล
    - แสดงรายการแบบ Portal ระดับบนสุด ไม่ถูกตัดหรือจมใต้ตารางและ Modal
    - ปรับมือถือให้ dropdown ในหน้ารายงานเรียงเต็มความกว้าง และขนาดตัวอักษรอ่านง่าย
- ผลลัพธ์: แอดมินค้นหาและเลือกข้อมูลจาก dropdown จำนวนมากได้เร็วขึ้น โดยไม่บังหรือทำให้ UI ล้นจอ

เรื่องที่ 10: เผยแพร่โค้ดขึ้ GitHub
  - ประเภท: Version Control และ Release
  - สิ่งที่ทำ:
    - รวมการปรับปรุง Frontend, Backend, CSS และรายงาน Daily ไว้ใน commit เดียว
    - Push ขึ้ branch `dev` ก่อน และ branch `main` ตามลำดับ
- ผลลัพธ์: ทั้ง `dev` และ `main` ใช้โค้ดชุดเดียวกันและพร้อมนำไปใช้งาน

เรื่องที่ 11: แก้ปัญหาหน้าเข้าสู่ระบบค้างและ Timeout
  - ประเภท: แก้ไขเร่งด่วนและปรับปรุงความเสถียร
  - สิ่งที่ทำ:
    - ตรวจสอบ API จริงและพบว่าคำขอ JSONP บางรอบค้างหรือได้รับข้อผิดพลาดจากปลายทาง Google
    - เพิ่มการเชื่อมต่อรอบสำรองอัตโนมัติหลังรอ 3 วินาที โดยแต่ละรอบใช้ callback และ cache-buster ใหม่
    - แสดงข้อความสั้นๆ ว่าระบบกำลังลองเชื่อมต่อใหม่ และคืนหน้าจอให้ผู้ใช้เสมอเมื่อสำเร็จหรือครบกำหนด
    - ลดการเปิด Google Spreadsheet ซ้ำภายในคำขอเดียวกัน
- ผลลัพธ์: เคสทดสอบที่เดิมใช้เวลาประมาณ 22 วินาที ตอบกลับได้ภายในประมาณ 7 วินาที และไม่ค้างอยู่ที่หน้ากำลังเข้าสู่ระบบ

เรื่องที่ 12: เพิ่มชื่อสถานที่จากพิกัด GPS
  - ประเภท: เพิ่มข้อมูลตำแหน่งและปรับโครงสร้างฐานข้อมูล
  - สิ่งที่ทำ:
    - เพิ่มฟิลด์ `location_name` ในชีต `ATTENDANCE` สำหรับเก็บตำบล อำเภอ และจังหวัด
    - เพิ่มการแปลงพิกัดเป็นชื่อสถานที่ภาษาไทย พร้อม cache ลดการค้นหาซ้ำ
    - แสดงสถานะกำลังค้นหาชื่อสถานที่ และยังอนุญาตให้บันทึกพิกัดได้หากค้นชื่อไม่สำเร็จ
    - แสดงชื่อสถานที่ใต้พิกัดในตารางแอดมิน และแสดงแบบตัวเล็กในประวัติฝั่งผู้ใช้
    - เพิ่มชื่อสถานที่ในไฟล์ Excel และ CSV ที่ส่งออก
- ผลลัพธ์: ผู้ใช้และแอดมินอ่านตำแหน่งได้เข้าใจง่ายขึ้น โดยข้อมูลเก่าที่ไม่มีฟิลด์นี้ยังใช้งานได้ตามปกติ

เรื่องที่ 13: ตั้งชื่อและเพิ่มไอคอนระบบอย่างเป็นทางการ
  - ประเภท: Branding และการแสดงผลบนอุปกรณ์
  - สิ่งที่ทำ:
    - ตั้งชื่อระบบเป็น `WorkLogs Attendance`
    - เพิ่ม favicon รูปโล่พร้อมเครื่องหมายถูกในรูปแบบ ICO, SVG และ PNG
    - เพิ่ม Apple Touch Icon และ Web App Manifest สำหรับ Bookmark และหน้าจอมือถือ
    - ใช้ไอคอนเดียวกันในหน้าเข้าสู่ระบบทั้ง Desktop และ Mobile
- ผลลัพธ์: ระบบมีชื่อและภาพลักษณ์เดียวกันทุกหน้า พร้อมแก้ข้อผิดพลาด `favicon.ico 404`

เรื่องที่ 14: แจ้งข่าวการอัปเดตเวอร์ชัน 1.3.2
  - ประเภท: Popup แจ้งเตือนผู้ใช้และแอดมิน
  - สิ่งที่ทำ:
    - เพิ่มข้อความแจ้งเตือนวันที่ `01/08/2026` แยกตามบทบาทผู้ใช้และแอดมิน
    - ใช้ข้อความสั้นที่คนทั่วไปเข้าใจได้ เช่น เข้าสู่ระบบเสถียรขึ้น แสดงชื่อสถานที่ และเพิ่มไอคอนระบบ
- ผลลัพธ์: ผู้ใช้แต่ละกลุ่มเห็นเฉพาะสรุปการเปลี่ยนแปลงที่เกี่ยวข้องเมื่อเปิดระบบเวอร์ชันใหม่

เรื่องที่ 15: เผยแพร่ Google Apps Script เวอร์ชัน 44
  - ประเภท: Deployment และตรวจสอบระบบจริง
  - สิ่งที่ทำ:
    - ตั้งชื่อโปรเจกต์ Backend เป็น `WorkLogs Attendance API`
    - อัปเดต deployment เดิมเป็น Apps Script เวอร์ชัน 44 โดยคง Web App URL และสิทธิ์ `ทุกคน` ไว้
    - เรียก API จริงเพื่อตรวจเวอร์ชัน 1.3.2, popup ของ user/admin และการค้นหาชื่อสถานที่ภาษาไทย
    - ยืนยันผลตัวอย่างเป็น `แขวงห้วยขวาง • เขตห้วยขวาง • กรุงเทพมหานคร`
- ผลลัพธ์: Backend เวอร์ชันใหม่พร้อมใช้งานจริง และ schema ใหม่ถูกตั้งค่าเรียบร้อยโดยไม่เปลี่ยน URL เดิม

เรื่องที่ 16: เร่งความเร็วหน้าแอดมินและผู้ใช้งาน เวอร์ชัน 1.3.3
  - ประเภท: Performance และแก้ไขข้อผิดพลาด
  - สิ่งที่ทำ:
    - เปลี่ยนคำขออ่านข้อมูลให้ใช้ `fetch` ผ่าน CORS ก่อน JSONP เพื่อลดเวลารอจากการ redirect ของ Google Apps Script
    - คง JSONP เป็นทางสำรอง และเก็บ callback ที่หมดเวลาไว้ชั่วคราวเพื่อไม่ให้เกิดข้อผิดพลาด `ReferenceError` จากคำตอบที่มาช้า
    - เปลี่ยนรายชื่อพนักงานให้ส่งเฉพาะข้อมูลย่อ ไม่ส่ง Base64 รูปโปรไฟล์ของทุกคนพร้อมกัน
    - โหลดรูปโปรไฟล์เฉพาะเมื่อแอดมินเปิดแก้ไขข้อมูล และโหลดรูปยืนยันตัวตนเฉพาะเมื่อกดดู
    - เก็บรูปขนาดใหญ่ไว้เฉพาะ memory cache เพื่อไม่ให้ LocalStorage เต็มและล้าง cache อื่นโดยไม่จำเป็น
    - โหลดรายงานช่วงวันที่ไม่เกิน 100 รายการครั้งเดียว แล้วกรองสาขา/พนักงานและเปลี่ยนหน้าในเครื่องทันที
    - เพิ่มสถานะกำลังดึงรายงาน/กำลังดึงประวัติในตำแหน่งตารางแทนการปล่อยหน้าจอค้าง
    - เริ่มตรวจสอบสถานะการลงเวลาและ GPS ก่อนโหลดไลบรารีกล้อง/แผนที่ เพื่อให้หน้า user ตอบสนองเร็วขึ้น
  - ผลลัพธ์: ลด payload รายชื่อพนักงานจากการวัดจริงประมาณ 402 KB ต่อครั้ง และลดการเรียก API ซ้ำเมื่อเปลี่ยน filter ในช่วงข้อมูลที่โหลดครบ

เรื่องที่ 17: เผยแพร่ Performance Update
  - ประเภท: Deployment และตรวจวัดผล
  - สิ่งที่ทำ:
    - อัปเดต Google Apps Script deployment เดิมเป็นเวอร์ชัน 45 โดยคง Web App URL และสิทธิ์เดิม
    - ทดสอบจาก API จริงหลังเผยแพร่: รายชื่อพนักงาน 19 คนส่งข้อมูลเพียง 3.5 KB และไม่มีรูปโปรไฟล์ติดมาด้วย
    - ทดสอบรูปยืนยันตัวตนของรายการจริงสำเร็จ พร้อมข้อมูลรูปขนาดประมาณ 31 KB
    - ทดสอบหน้าเข้าสู่ระบบบนเบราว์เซอร์จากต้นฉบับใหม่ ยืนยันว่า CORS fetch รับคำตอบได้และแสดงผลล้มเหลวตามปกติ
- ผลลัพธ์: ลด payload รายชื่อพนักงานจากเดิมประมาณ 406 KB เหลือประมาณ 3.5 KB และตัดปัญหา JSONP callback ที่หมดอายุเป็นเส้นทางหลัก

เรื่องที่ 18: แก้ปัญหากล้องและสถานะลงเวลาไม่ถูกต้อง
  - ประเภท: แก้ไขเร่งด่วนและป้องกันข้อมูลลงเวลาผิดพลาด
  - สิ่งที่ทำ:
    - แก้ระบบกล้องให้ใช้ media stream เพียงชุดเดียวสำหรับภาพพรีวิวและตรวจจับใบหน้า จึงไม่เปิดกล้องซ้ำจนชนกันบนคอมพิวเตอร์
    - แก้การเปรียบเทียบวันที่ประวัติลงเวลาให้รองรับรูปแบบ `DD/MM/YYYY HH:mm:ss` ทำให้ตรวจพบรายการเข้างานของวันปัจจุบันได้ถูกต้อง
    - ระหว่างโหลดสถานะ ปุ่มลงเวลาจะแสดง `กำลังตรวจสอบ` และกดไม่ได้; เมื่อพบว่าเข้างานแล้วจะแสดง `เข้าแล้ว` และเปิดเฉพาะปุ่ม `ออกงาน`
    - เพิ่มการตรวจสอบลำดับ `เข้างาน → ออกงาน` ใน Google Apps Script เพื่อปฏิเสธการเข้างานซ้ำ แม้เปิดหลายแท็บหรือมีหน้าจอเก่า
    - ลบข้อความ `Secured by Antigravity` ออกจากหน้าเข้าสู่ระบบ
  - ผลลัพธ์: ผู้ที่เข้างานแล้วไม่เห็นหรือกดปุ่มเข้างานซ้ำ และสามารถดำเนินการออกงานได้ตามลำดับที่ถูกต้อง พร้อมลดปัญหากล้องดำบนคอมพิวเตอร์

เรื่องที่ 19: เผยแพร่ Hotfix สถานะลงเวลา
  - ประเภท: Deployment และ Version Control
  - สิ่งที่ทำ:
    - เผยแพร่ Google Apps Script เวอร์ชัน 46 เป็น Web App โดยคงสิทธิ์เข้าถึง `ทุกคน`
    - อัปเดต Frontend ให้เรียก URL ของ Web App เวอร์ชันใหม่ และเพิ่ม query version ของ `app.js` เพื่อเลี่ยงไฟล์ cache เก่า
    - สร้าง commit `32e04ba fix: prevent duplicate check-in and camera conflicts`
    - Push ชุด Hotfix เดียวกันไปยัง branch `dev` และ `main` โดยแยกจากงานที่ยังแก้ไขไม่เสร็จ
- ผลลัพธ์: Backend และ Frontend ที่เผยแพร่ใช้งานชุดแก้ไขเดียวกัน โดยงานค้างในโฟลเดอร์หลักไม่ได้ถูกรวมใน Release นี้

เรื่องที่ 20: เร่งความเร็วหน้าเว็บและแก้การดูรูปยืนยันตัวตน
  - ประเภท: Performance, ลดข้อมูลซ้ำซ้อน และแก้ไขเสถียรภาพ
  - สิ่งที่ทำ:
    - ลดข้อมูลรายชื่อพนักงานจากประมาณ 406 KB เหลือ 3.5 KB โดยดึงรูปโปรไฟล์เฉพาะตอนเปิดแก้ไขข้อมูล
    - ใช้ `fetch` เป็นช่องทางหลักและเก็บ JSONP ไว้เป็นสำรอง แก้ callback ที่ตอบกลับช้าจนเกิด Timeout หรือ `ReferenceError`
    - แคชรูปยืนยันตัวตนในหน่วยความจำและแคชตำแหน่งแถวฝั่งระบบ เพื่อลดการค้นหา Sheet ซ้ำเมื่อกดดูรูป
    - กรองสาขา/พนักงานและเปลี่ยนหน้ารายงานจากข้อมูลที่โหลดแล้วทันทีเมื่อมีไม่เกิน 100 รายการ พร้อมสถานะ `กำลังดึงข้อมูล` สำหรับรายการที่ยังต้องเรียก API
    - เพิ่มเลขเวอร์ชันไฟล์หน้าเว็บเป็น `20260801-7` เพื่อให้ผู้ใช้ได้รับ JavaScript ชุดใหม่ ไม่ติด cache เก่า
    - อัปเดต Google Apps Script deployment เดิมเป็นเวอร์ชัน 47 โดยคง URL และสิทธิ์ `ทุกคน`
  - ผลลัพธ์: หน้า admin และ user ลดเวลารอจาก payload ที่เกินจำเป็น, การกรองไม่รอ API ซ้ำในชุดข้อมูลขนาดเล็ก, และหน้าดูรูปไม่ล้มเหลวเพราะ callback เก่าหมดอายุ

เรื่องที่ 21: แก้หน้าต่างเพิ่มและแก้ไขข้อมูลพนักงาน
  - ประเภท: แก้ไข UI, Responsive และความสะดวกในการใช้งาน
  - สิ่งที่ทำ:
    - นำปุ่มสร้างข้อมูลสมมติและทดสอบระบบที่แสดงผิดตำแหน่งออกจากหน้าต่างแก้ไขพนักงาน
    - จำกัดขนาดหน้าต่างให้อยู่ในจอ พร้อมเลื่อนเฉพาะเนื้อหาภายในบนคอมพิวเตอร์และมือถือ
    - เพิ่มปุ่ม X และรองรับการคลิกพื้นที่ด้านนอกหรือกด Escape เพื่อปิดหน้าต่าง
    - เปลี่ยนข้อความเป็น `Username เข้าใช้งานระบบ` และแสดงรหัสผู้ใช้ให้เห็นชัดเจน
    - คืนการแสดงรหัสผ่านเดิมในหน้าแก้ไข โดยดึงเฉพาะตอนเปิดรายการเพื่อลดข้อมูลที่โหลดหน้าแอดมิน
    - ตรวจสอบ Dropdown สาขาและสิทธิ์ให้พิมพ์ค้นหาได้โดยไม่จมใต้หน้าต่าง
  - ผลลัพธ์: หน้าต่างเพิ่ม/แก้ไขพนักงานใช้งานได้ครบ ไม่ล้นจอ และปิดได้สะดวกทั้งคอมพิวเตอร์และมือถือ

เรื่องที่ 22: แก้การเปิดกล้องให้เริ่มทำงานเร็วขึ้น
  - ประเภท: แก้ไขเร่งด่วนและปรับปรุง Performance
  - สิ่งที่ทำ:
    - เปิด Camera Preview ทันทีโดยไม่รอไลบรารีแผนที่และตรวจจับใบหน้าจาก CDN
    - โหลดไลบรารีแผนที่และตรวจจับใบหน้าพร้อมกันเพื่อลดเวลารอ
    - เพิ่มเงื่อนไขสำรองให้ถ่ายรูปได้เมื่อระบบตรวจจับใบหน้าโหลดช้าหรือหยุดตอบสนอง
    - เพิ่มข้อผิดพลาดภาษาไทยที่เข้าใจง่ายสำหรับกรณีไม่อนุญาตกล้อง ไม่พบกล้อง หรือกล้องถูกโปรแกรมอื่นใช้งาน
    - ตรวจสอบการถ่ายใหม่ให้กลับมาตรวจจับใบหน้าหรือเปิดโหมดถ่ายรูปสำรองได้ตามปกติ
  - ผลลัพธ์: กล้องบนคอมพิวเตอร์และมือถือไม่ถูกการโหลด CDN ขวาง และผู้ใช้ยังถ่ายรูปได้เมื่อส่วนตรวจจับใบหน้าช้า

เรื่องที่ 23: เผยแพร่ Hotfix เวอร์ชัน 1.3.4
  - ประเภท: Deployment, QA และ Version Control
  - สิ่งที่ทำ:
    - ทดสอบ Modal จริงที่ขนาดจอ 1302×1072 และ 390×844 รวมถึงปุ่ม X, คลิกนอกหน้าต่าง และ Dropdown แบบค้นหา
    - ตรวจสอบว่า Console ไม่มีข้อผิดพลาดในเส้นทางเพิ่มและแก้ไขพนักงาน
    - เผยแพร่ Google Apps Script deployment เดิมเป็นเวอร์ชัน 51 โดยคง Web App URL และสิทธิ์เดิม
    - ตรวจสอบ API จริงของเวอร์ชัน, รายละเอียดผู้ใช้ และข้อความแจ้งอัปเดตหลังเผยแพร่
    - Push โค้ดชุดเดียวกันไปยัง branch `dev` และ `main`
  - ผลลัพธ์: ระบบใช้งาน Hotfix ชุดเดียวกันทั้ง Backend และหน้าเว็บ พร้อมหลักฐานการทดสอบ Responsive
